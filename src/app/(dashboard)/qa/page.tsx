import { QaListPageClient } from '@/features/qa';

/**
 * Danh sách Q&A — UI client (Ant Design); metadata tại `layout.tsx`.
 */
export default function QaListPage() {
  return <QaListPageClient />;
}
