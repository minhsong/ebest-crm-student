/**
 * @deprecated Dùng `@/lib/html-audio-session` — giữ re-export cho quiz feature.
 */
export {
	isHtmlAudioSessionUnlocked as isQuizAudioSessionUnlocked,
	resetHtmlAudioSessionUnlock as resetQuizAudioSessionUnlock,
	unlockHtmlAudioSession as unlockQuizAudioSession,
} from '@/lib/html-audio-session';
