/** Số lần phát US mỗi thẻ trong chế độ Auto Play. */
export const FLASHCARD_AUTO_PLAY_REPEATS = 3;

/** Nghỉ giữa các lần phát (ms). */
export const FLASHCARD_AUTO_PLAY_GAP_MS = 3000;

/** Nghỉ sau lần phát cuối trước khi chuyển thẻ (ms). */
export const FLASHCARD_AUTO_PLAY_ADVANCE_GAP_MS = 5000;

/** Delay auto-play mặt trước khi chưa lật thẻ (ms). */
export const FLASHCARD_FRONT_AUTO_PLAY_DELAY_MS = 300;
