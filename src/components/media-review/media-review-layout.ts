/** Video trong modal xem nhận xét — chiều cao tối đa (px). */
export const PORTAL_VIDEO_MAX_HEIGHT = 250;

/** Vùng scroll danh sách mốc nhận xét — chiều cao tối thiểu (px). */
export const PORTAL_COMMENT_LIST_MIN_HEIGHT = 150;
